def avg
in {avg = (x#1 => (def x#2, x, y
                   in {x#2 = x#1;
                       x = int[x#2[0]];
                       y = int[x#2[1]];
                       operator'/'[operator'+'(x, y), 2]}));
    print(avg(2, 4))}
