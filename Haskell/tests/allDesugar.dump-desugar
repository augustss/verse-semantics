{x:= :int;
 x#1 => x#1 = 1 | 2;
 (x, operator'>'[x, 0])[0];
 let (x#2:= operator'+'(x, 1))
 in
   if (x#5:= (x#6 => {x#6 = 0;
                      1})[x#2])
   then x#5
   else (if (x#4:= (x#7 => {x#7 = 1;
                            1})[x#2])
         then x#4
         else (if (x#3:= (n => operator'-'(n, 1))[x#2])
               then x#3
               else :false));
 if (x) then y else :false;
 if (x#8:= x) then x#8 else y;
 x:= int[e];
 {x#9:= e;
  x:= x#9[0];
  y:= x#9[1]};
 f:= x#10 => {x#10 = (x:= :int);
              e};
 operator'+'(x, y);
 operator'<'[x, y]}
