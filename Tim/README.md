This directory contains material written by Tim (Spec and TimVerse) and our attempts to formalize the same.

NOTE: `Haskell/Spec` contains duplicates of `TimVerse/spec`, the latter are authoritative.

Tim/
  TimeVerse/  (C++ interpreter and verifier)
  Haskell/
    Spec/     (used to be TimNotes)
    Runtime   (used to be TimRuntime)
    Verifier  (used to be TimVerification)

(JanV, Sept 2024)
