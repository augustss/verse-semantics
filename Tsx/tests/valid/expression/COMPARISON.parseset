#! Valid expression
# 1. Array type (not a map type, but valid syntax)
arrayMap: []int = GetMap()


#! Valid expression
# 2. Array type with spaces (valid syntax)
arrayWithSpaces: [  ]int = GetMap()


#! Valid expression
# 3. Lambda with array access
i => arr[i]


#! Valid expression
# 4. Lambda in array
array{x => x + 1, y => y * 2}


#! Valid expression
# 5. Multiple variable declarations with arrays
{
  var x : int = 10;
  var y : int = 20;
  x + y
}


#! Valid expression
# 6. Nested spawn
spawn{spawn{InnerTask()}}


#! Valid Program
# 7. Spawn in function
TestSpawn():void=
    spawn{BackgroundWork()}
    Print("Main thread continues")


#! Valid Program
# 8. Multiple spawns in sequence
RunTasks():void=
    spawn{TaskA()}
    spawn{TaskB()}
    spawn{TaskC()}


#! Valid expression
# 9. Spawn as expression value
GetSpawnResult():spawn_type = spawn{DoWork()}


#! Valid expression
# 10. Race as expression value
FirstToComplete:result_type = race:
    SlowComputation()
    FastComputation()


#! Valid expression
# 11. Sync as expression value
TransactionResult:bool = sync:
    UpdateDatabase()
    true


#! Valid expression
# 12. Allow if expression
if(x > 0) then { x }


#! Valid expression
# 13. If expression with numeric conditions
if(count > 0) then { count } else { 0 }


#! Valid expression
# 14. Comparison in if expression
if(x > 0) then positive else nonPositive


#! Valid expression
# 15. If expression with parenthesized condition
if((x + y) > threshold) then { action() }


#! Valid expression
# 16. If expression with complex condition
if(x > 0 and y < 10) then { process(x, y) }


#! Valid expression
# 17. If expression with else if brace style
if(x > 0) then { positive } else if(x < 0) then { negative } else { zero }


#! Valid expression
# 18. If with complex condition
if ( x > 5 ) then { x * 2 }


#! Valid expression
# 19. Precedence in if condition
if(a + b > c * d) then: result


#! Valid expression
# 20. Complex conditional with nested operations
if(((player.health + bonus) * modifier) > ((enemy.damage - armor) * threat)):
  player.attack(enemy)
else:
  player.defend()


#! Valid expression
# 21. Indented if expression with else if
if(x > 0) then:
  positive
else if(x < 0) then:
  negative
else:
  zero


#! Valid expression
# 22. For loop with indexed iteration
for(i -> x : items) { x }


#! Valid expression
# 23. For-loop with index
for(i -> x : items) { x + i }


#! Valid expression
# 24. For with if expression in body
for(x : items) { if(x > 0) then x else -x }


#! Valid expression
# 25. For loop with indexed expression body
for(idx -> item : collection) { item * idx }


#! Valid expression
# 26. For loop with index-value pattern
for (index -> value : myArray):
    Process(value)


#! Valid expression
# 27. For-loop with if expression in body
for(x : items) {
  if (x > 0) then { x } else { -x }
}


#! Valid expression
# 28. Indented for loop with indexed iteration
for(i -> x : items):
  x + i


#! Valid expression
# 29. Simple case expression
case(x):
  0 => y


#! Valid expression
# 30. Case with single branch
case(x) { 1 => a }


#! Valid expression
# 31. Case with multiple branches
case(x) { 1 => a, 2 => b, _ => c }


#! Valid expression
# 32. Case with many branches
case(x):
  0 => a
  1 => b
  2 => c
  3 => d
  4 => e
  _ => f


#! Valid expression
# 33. Test case
case(x):
    0 => "zero"
    _ => "other"


#! Valid expression
# 34. Test case
case(value) { 0 => result, _ => default }


#! Valid expression
# 35. Test case
for(i -> item : items):
    case(i):
        0 => item.Process()
        _ => item.Skip()


#! Valid expression
# 36. Nested loops with break
for(i : 0..10) {
  for(j : 0..10) {
    if(i + j > 10) then { break }
  }
}


#! Valid expression
# 37. Break and continue in for loop
for(i : 0..10) {
  if(i > 5) then { break } else { continue }
}


#! Valid expression
# 38. Nested loops
loop {
  for(i : 0..10) {
    if(i > 5) then { break }
  }
}


#! Valid expression
# 39. Brace block with var declarations
{ var x: int = 5; x * 2 }


#! Valid expression
# 40. Block with var declarations
{ var x : int = 5; x * 2 }


#! Valid expression
# 41. Const with type and complex value
sum : int = a + b


#! Valid expression
# 42. Const with type and function call
data : string = getName()


#! Valid expression
# 43. Allow var declaration
var x:int = 5


#! Valid expression
# 44. Option type as identifier
field : option = Some(5)


#! Valid expression
# 45. Option type in function parameter
ProcessOption(value: option): void = Print(value)


#! Valid expression
# 46. Option type variable
maybe_list : option = None


#! Valid expression
# 47. Option type with var declaration
var maybe_value : option = Some(3.14)


#! Valid expression
# 48. Option type in complex expression
result : option = if (x > 0) then Some(player) else None


#! Valid expression
# 49. Parameterized type with parentheses
var GlobalInt:weak_map(session, int) = map{}


#! Valid expression
# 50. Type with angle bracket parameters
field : option<int> = None


#! Valid expression
# 51. Comparable type usage
value : comparable = 42


#! Valid expression
# 52. Rational type usage
ratio : rational = CreateRatio(3, 4)


#! Valid expression
# 53. Function with visibility specifier
Foo<public>(X:int):int = X > 0


#! Valid expression
# 54. Function with visibility and post specifiers
Bar<private>(Y:string)<decides>:string = ProcessString(Y)


#! Valid expression
# 55. Var declaration with underscore in name
var my_variable : int = 100


#! Valid expression
# 56. Variable with complex expression
var sum : int = x + y


#! Valid expression
# 57. Var declaration with boolean type
var flag : logic = true


#! Valid expression
# 58. Variable with parenthesized expression
var value : int = (a + b) - c


#! Valid expression
# 59. Variable with block value
var result : int = { x + y; z }


#! Valid expression
# 60. Var declaration with expression
var result : int = x + y * 2


#! Valid expression
# 61. Var declaration with string type
var message : string = "Hello World"


#! Valid expression
# 62. Var declaration with function call
var value : int = calculateSum(a, b)


#! Valid expression
# 63. Variable with path specifier
var config <scoped{config.json}> : Config = loadConfig()


#! Valid expression
# 64. Const with type and spaces
x   :   int   =   42


#! Valid expression
# 65. Set statement with expression
set result = x + y * 2


#! Valid expression
# 66. Set statement with variable containing underscore
set my_var = 100


#! Valid expression
# 67. Set statement with boolean expression
set flag = x > 5 and y < 10


#! Valid expression
# 68. Set statement with string literal
set message = "Hello World"


#! Valid expression
# 69. Set statement with function call
set value = calculateSum(a, b)


#! Valid expression
# 70. Set statement with complex expression
set final = (x + y) * (z - w) / 2


#! Valid expression
# 71. Set array element
set arr[0] = 42


#! Valid expression
# 72. Set array element with expression index
set matrix[i + 1] = value


#! Valid expression
# 73. Set nested array element
set grid[x][y] = newValue


#! Valid expression
# 74. Set array element with spaces
set x[ 21 ] = 123


#! Valid expression
# 75. Set map element
set scores["player1"] = 100


#! Valid expression
# 76. Set nested member array element
set obj.data[index] = updated


#! Valid expression
# 77. Function with return type
f() : int = 42


#! Valid expression
# 78. Function with args and return type
add(x, y) : int = x + y


#! Valid expression
# 73. Function with complex argument expressions
transform(
  source.map(x => x + 1),
  target.filter(y => y > 0),
  combine(a, b, c)
)


#! Valid expression
if (X > 0). Print("Positive")


#! Valid expression
if (X > 0). Print("Positive") else. Print("Non-positive")


#! Valid expression
if (Count >= 10). Reset(); Log("Reset occurred")


#! Valid expression
if (GameState == Running). Update(); Render()


#! Valid expression
if (Health <= 0). Die() else. Continue()


#! Valid expression - nested conditions
if (A > B). if (C < D). DoThis() else. DoThat()


#! Valid expression
# 80. Greater than without spaces
x>0


#! Valid expression
# 81. Less than without spaces
x<10


#! Valid expression
# 82. Greater equal without spaces
x>=5


#! Valid expression
# 83. Less equal without spaces
x<=20


#! Valid expression
# 84. Multiple comparisons without spaces
x>0 and y<10


#! Valid expression
# 85. Complex comparison without spaces
x+1>y*2


#! Valid expression
# 86. Comparison with function calls and spaces
f(x) > g(y)


#! Valid expression
# 87. Comparison with function calls without spaces
f(x)>g(y)


#! Valid expression
# 88. Comparison with member access and spaces
obj.a > obj.b


#! Valid expression
# 89. Comparison with member access without spaces
obj.a>obj.b


#! Valid expression
# 90. Variables with comparison operators and spaces
a > b


#! Valid expression
# 91. Variables with comparison operators without spaces
a>b


#! Valid expression
# 92. Lambda with spaces
x  =>  x + 1


#! Valid expression
# 93. For loop with whitespace in indexed form
for( i -> x : items ) { x + i }


#! Valid expression
# 94. Comparison with float literals
x > 1.5


#! Valid expression
# 95. Less than
x < 10


#! Valid expression
# 96. Greater than or equal
x >= 5


#! Valid expression
# 97. Less than or equal
x <= 20


#! Valid expression
# 98. Chained comparisons
a > b > c


#! Valid expression
# 99. Comparison with arithmetic
x + 1 > y - 1


#! Valid expression
# 100. Comparison with member access
item.value > threshold


#! Valid expression
# 101. Comparison with method call
getValue() < getLimit()


#! Valid expression
# 102. Multiple comparisons with logical
x > 0 and x < 10


#! Valid expression
# 103. Comparison with parentheses
(x + y) > (a + b)


#! Valid expression
# 104. Mixed spacing in comparisons
x>0 and y < 10


#! Valid expression
# 105. Complex comparison with arithmetic
x + 1 > y * 2


#! Valid expression
# 106. Not with comparison
not (x > 0)


#! Valid expression
# 107. Logical with member access
item.isValid and item.value > 0


#! Valid expression
# 108. Member access in comparison
item.price > 100


#! Valid expression
# 109. Comparison operators
1 < 2


#! Valid expression
# 110. Comparison with arithmetic
1 + 2 < 3 * 4


#! Valid expression
# 111. Multiple comparisons (should be left associative)
a < b < c


#! Valid expression
# 112. Lambda with multiplication
n => n * 2


#! Valid expression
# 113. Lambda with member access
item => item.getValue()


#! Valid expression
# 114. Lambda with function call
p => f(p, q)


#! Valid expression
# 115. Lambda with parenthesized expression
n => (n + 1) * 2


#! Valid expression
# 116. Nested lambdas (currying)
a => b => a + b


#! Valid expression
# 117. Triple nested lambdas
a => b => c => a + b + c


#! Valid expression
# 118. Lambda as reduce argument
reduce(acc => acc + 1)


#! Valid expression
# 119. Lambda as filter argument
filter(item => item.isValid)


#! Valid expression
# 120. Multiple lambdas in call
process(x => x * 2, y => y + 1)


#! Valid expression
# 121. Lambda with method chain
x => x.trim().toUpper().split()


#! Valid expression
# 122. Lambda with conditional
x => if(x > 0) then { x } else { -x }


#! Valid expression
# 123. Lambda chained with call
transform(data.map(x => x + 1))


#! Valid expression
# 124. Lambda with filter and map
process(items.filter(i => i.active).map(i => i.value))


#! Valid expression
# 125. Lambda in expression
1 + (x => x + 1)(5)


#! Valid expression
# 126. Lambda with immediate application
(x => x * 2)(10)


#! Valid expression
# 127. Lambda returning lambda
x => (y => x + y)


#! Valid expression
# 128. Deeply nested structures
for(a : items):
  for(b : items):
    if(a > b) then:
      if(b > 0) then:
        log("nested")


#! Valid expression
# 129. Nested conditional with complex expressions
if((x + y) > (a * b)) then:
  calculate(x, y)
else:
  if((x - y) < (a / b)) then:
    fallback(x)
  else:
    default()


#! Valid expression
# 130. Test case
<# comment #>42


#! Valid expression
# 131. Test case
42<# comment #>


#! Valid expression
# 132. Test case
<# comment #>42<# another #>


#! Valid expression
# 133. Test case
x <# inline #> + <# another #> y


#! Valid expression
# 134. Test case
<# multi
line
comment #>x + y


#! Valid expression
# 135. Test case
f(<# before arg #>x<# after arg #>)


#! Valid expression
# 136. Test case
f(<# c1 #>x<# c2 #>, <# c3 #>y<# c4 #>)


#! Valid expression
# 137. Test case
<# start #>(<# before expr #>x + y<# after expr #>)<# end #>


#! Valid expression
# 138. Test case
<#comment#>1<#c#>+<#c#>2<#c#>


#! Valid expression
# 139. Test case
x + y # line comment
<# also multiline #> + z


#! Valid expression
# 140. Less than or equal comparison
a <= b


#! Valid expression
# 141. Equality check
value = 10


#! Valid expression
# 142. Complex expression with multiple keywords
if(true) then { array{1,2,3} } else {  { var x:int = 5; x } }


#! Valid expression
# 143. Nested if statements with multiple statements
for(x : items):
  if(a > 5) then:
    log("first")
    log("second")


#! Valid expression
# 144. Nested if-else in for loop
for(item : items):
  if(item > 5) then:
    log("big")
  else:
    log("small")


#! Valid expression
# 145. Case expression with empty lines (if supported)
case(x):
  0 =>
    first()

    result1

  1 =>
    second()

    result2

  _ =>
    default()

    defaultResult


#! Valid expression
# 146. If expression with colon style
if (x > 0):
    DoSomething()


#! Valid expression
# 147. For with multiline comment
for(<# iterator #> x : items) { <# body #> x }


#! Valid expression
# 148. For loop with comments
for(x <# item #> : items <# collection #>) { <# body #> x }


#! Valid expression
# 149. If expression with comments
if(condition <# check #>) then { <# then #> value } else { <# else #> fallback }


#! Valid expression
# 150. Multi-line comment with spacing (exact format)
<# This is a
  multi-line comment #>
x + y


#! Valid expression
# 151. Variable with comments
var x # name
  : int # type
  = 42 # value


#! Valid expression
# 152. Variable with multiline comment
var x <# variable #> : int = 42


#! Valid expression
# 153. Comments with comparisons
x > # greater than
0 and # and
y < # less than
10


#! Valid expression
# 154. Multiline comments
x <# greater #> > <# zero #> 0


#! Valid expression
# 155. Multiline comments with literals
<# int #>42 + <# float #>3.14


#! Valid expression
# 156. Multiline comments
x <# and #> and <# y #> y


#! Valid expression
# 157. Multiline comments
x.<# member #>y


#! Valid expression
# 158. Lambda with inline comments
x <# comment #> => <# another #> x + 1


#! Valid expression
# 159. Lambda with line comments
x => # increment
x + 1


#! Valid expression
# 160. Minimal comment
<##> x


#! Valid expression
# 161. Very long comment
<# This is an extremely long comment that tests the parser's ability to handle lengthy comment blocks without any issues or problems whatsoever and should work correctly #>42


#! Valid expression
# 162. Comment with special characters
<# !@#$%^&*()_+-=[]{}|;:'"<>?,./ #>x


#! Valid expression
# 163. Nested comment delimiters (but not actual nesting)
<# comment with <# and #> inside #>x


#! Valid expression
# 164. Multiple line comments
<# comment 1 #><# comment 2 #><# comment 3 #>x


#! Valid expression
# 165. Mixed comment styles
<# block comment #>x


#! Valid expression
# 166. Mixed comment styles with complex expression
<# Block comment #>
calculate( # inline comment
  x + y, # argument 1
  a * b  # argument 2
) # end comment


#! Valid expression
# 167. Empty line with mixed comment styles
if(condition) then:
  <# block comment #>

  # line comment

  result
else:
  default


#! Valid expression
# 168. Nested comments with expressions
<# outer comment #>
f(
  <# inner comment #>
  g(x) + h(y) <# end comment #>
)


#! Valid expression
# 169. Comments with surrounding whitespace
x  <# comment #>  + y


#! Valid expression
# 170. Simple multi-line comment
<# comment #>
x + y


#! Valid expression
# 171. Multi-line comment with newlines
<# This spans
multiple lines #>
x + y


#! Valid expression
# 172. Nested multi-line comments
<# outer <# inner #> outer #>
x + y


#! Valid expression
# 173. Mixed single and multi-line comments
<# Block comment #>
x + y


#! Valid expression
# 174. Multi-line comment at end
x + y <# trailing comment #>


#! Valid expression
# 175. Empty multi-line comment
<##>
x + y


#! Valid expression
# 176. Multi-line comment with special characters
<# Comment with symbols: @#$%^&*()[]{}|\ #>
x + y


#! Valid expression
# 177. Const with type and comments
x # name
  : int # type
  = 42 # value


#! Valid expression
# 178. Function with return type and comments
add # function
  (x, y) # params
  : int # return type
  = # equals
  x + y # body


#! Valid expression
# 179. continue as function with parameters
continue(x: int, y: string): logic = x > 0


#! Valid expression
# 180. continue as parameter name
process(continue: int): int = continue * 2


#! Valid expression
# 181. Basic map type declaration
userMap: [string]int = GetDefaultMap()


#! Valid expression
# 182. Map with complex key type
complexMap: [UserID]UserData = CreateUserMap()


#! Valid expression
# 183. Map with array value type
arrayMap: [string][]int = GetStringToArrayMap()


#! Valid expression
# 184. Map with optional value type
optionalMap: [string]?User = GetOptionalUserMap()


#! Valid expression
# 185. Nested map types
nestedMap: [string][int]User = GetNestedMap()


#! Valid expression
# 186. Optional map type
maybeMap: ?[string]int = GetMapIfAvailable()


#! Valid expression
# 187. Array of maps
mapArray: [][string]User = CreateMapArray()


#! Valid expression
# 188. Function parameter with map type
ProcessUsers(users: [UserID]User): void = {}


#! Valid expression
# 189. Function returning map type
GetScores(): [string]int = CreateScoreMap()


#! Valid expression
# 190. Map with tuple key type
tupleKeyMap: [tuple(int, string)]User = CreateTupleMap()


#! Valid expression
# 191. Map with tuple value type
tupleValueMap: [string]tuple(int, string) = CreateValueMap()


#! Valid expression
# 192. Map with generic-style key type
genericMap: [option<string>]int = CreateGenericMap()


#! Valid expression
# 193. Map with parameterized key type
weakMap: [weak_map(session, string)]int = GetWeakMap()


#! Valid expression
# 194. Complex nested map type
complexNested: [string]?[int][]tuple(string, User) = GetComplexMap()


#! Valid expression
# 195. Map type variable declaration
var userCache: [string]User = EmptyMap()


#! Valid expression
# 196. Map type assignment
scoreMap: [string]int = GetScoreMap()


#! Valid expression
# 197. Multiple map parameters
MergeUserMaps(primary: [UserID]User, secondary: [UserID]User): [UserID]User = CombineMaps(primary, secondary)


#! Valid expression
# 198. Complex boolean logic
(age >= 18 and age <= 65) and (not retired or hasException)


#! Valid expression
# 199. Visibility specifier separated from behavior specifier
VisibilityBehavior<public>(x:int)<decides>:int = ProcessValue(x)


#! Valid expression
# 200. Only visibility specifier after function name
VisibilityOnly<private>():int = 42


#! Valid expression
# 201. Visibility specifier followed by separate behavior specifier
SeparateGroups<public><override>():void = DoWork()


#! Valid expression
# 202. Behavior specifiers only after parameters
BehaviorOnly(x:int)<decides>:logic = ValidateAsync(x)


#! Valid expression
# 203. Multiple separate specifier groups
MultipleGroups<protected><final>(x:int)<decides>:void = Process(x)


#! Valid expression
# 204. Internal visibility with behavior specifiers
InternalFunction<internal>(data:string)<decides>:string = ProcessComplex(data)


#! Valid expression
# 205. Scoped visibility with parameters
ScopedFunction<scoped>(x:int, y:string)<decides>:logic = ValidateInputs(x, y)


#! Valid expression
# 206. Function with visibility and array parameters
ArrayProcessor<public>(items:[]int, filters:[]Filter)<decides>:[]int = FilterItems(items, filters)


#! Valid expression
# 207. Function with visibility and optional parameters
OptionalProcessor<protected>(required:int, optional:?string)<decides>:?string = ProcessOptionalArray(required, optional)


#! Valid expression
# 208. Function with visibility and complex return type
SecurityCheck<scoped>(credentials:Credentials, resource:Resource)<decides>:AccessLevel = ValidateAccess(credentials, resource)


#! Valid expression
# 209. Performance-critical method with visibility and behavior
FastPath<public>(data:[]byte)<inline>:int = CountBytes(data)


#! Valid expression
# 210. Simple tuple type declaration
coords: tuple(int, int) = (10, 20)


#! Valid expression
# 211. Three-element tuple type
position: tuple(float, float, float) = (1.0, 2.0, 3.0)


#! Valid expression
# 212. Mixed type tuple
person: tuple(string, int, logic) = ("Alice", 25, true)


#! Valid expression
# 213. Nested tuple types
matrix: tuple(tuple(int, int), tuple(int, int)) = ((1, 2), (3, 4))


#! Valid expression
# 214. Function with tuple parameter
ProcessPoint(p: tuple(int, int)): void = {}


#! Valid expression
# 215. Function returning tuple type
GetCoordinates(): tuple(float, float) = (0.0, 0.0)


#! Valid expression
# 216. Optional tuple type
maybeCoords: ?tuple(int, int) = false


#! Valid expression
# 217. Array of tuples
points: []tuple(int, int) = array{}


#! Valid expression
# 218. Tuple with complex types
data: tuple([]int, string, ?float) = (array{1, 2, 3}, "test", 1.5)


#! Valid expression
# 219. Function with multiple tuple parameters
Transform(from: tuple(float, float), to: tuple(float, float)): tuple(float, float) = from


#! Valid expression
# 220. Generic-like tuple type
KeyValue: tuple(string, any) = ("key", 42)


#! Valid expression
# 221. Tuple with option types
result: tuple(string, ?int, logic) = ("success", 100, true)


#! Valid expression
# 222. Complex nested tuple type
complexData: tuple(tuple(string, int), []tuple(float, float)) = (("test", 1), array{})


#! Valid expression
# 223. Simple function call in type
Foo() : int = 0
Bar(X : type{Foo()}) : type{Foo()} = X


#! Valid expression
# 224. Type with identifier expression
getValue() : int = 42
result : type{getValue()} = getValue()


#! Valid expression
# 225. Type with binary expression
calculate(x : int, y : int) : int = x + y
operation : type{calculate(1, 2)} = calculate(3, 4)


#! Valid expression
# 226. Type with member access
obj.method() : string = "test"
memberType : type{obj.method()} = obj.method()


#! Valid expression
# 227. Type with array access
arr[0] : int = getValue()
arrayType : type{arr[0]} = arr[1]


#! Valid expression
# 228. Type with compound expression
process() : void = { print("done") }
blockType : type{process()} = process()


#! Valid expression
# 229. Type with conditional expression
getValue(flag : logic) : int = if flag then 1 else 0
conditionalType : type{getValue(true)} = getValue(false)


#! Valid expression
# 230. Type with tuple expression
makePair(x : int, y : int) : tuple(int, int) = (x, y)
tupleType : type{makePair(1, 2)} = makePair(3, 4)


#! Valid expression
# 231. Function parameter with type{expression}
processValue(callback : type{_() : int}) : int = callback()


#! Valid expression
# 232. Function return type with type{expression}
getProcessor() : type{_() : int} = getValue


#! Valid expression
# 233. Variable declaration with type{expression}
var processor : type{getValue()} = getValue()


#! Valid expression
# 234. Type{expression} with nested function calls
outer(inner() : int) : int = inner()
nestedType : type{outer(getValue())} = outer(getValue())


#! Valid expression
# 235. Optional type{expression}
maybeProcessor : ?type{getValue()} = getValue()


#! Valid expression
# 236. Array of type{expression}
processors : []type{getValue()} = array{getValue, getValue}


#! Valid expression
# 237. Map with type{expression} values
processMap : [string]type{getValue()} = map{"key" => getValue}


#! Valid expression
# 238. Function with public visibility specifier
PublicFunc<public>():int = 42


#! Valid expression
# 239. Function with private visibility specifier
PrivateFunc<private>():string = "secret"


#! Valid expression
# 240. Function with protected visibility specifier
ProtectedFunc<protected>():logic = true


#! Valid expression
# 241. Function with internal visibility specifier
InternalFunc<internal>():void = {}


#! Valid expression
# 242. Function with scoped visibility specifier
ScopedFunc<scoped>():float = 3.14


#! Valid expression
# 243. Function with visibility and behavior specifiers
PublicDecides<public>(x:int)<decides>:logic = x > 0


#! Valid expression
# 244. Function with private and multiple behavior specifiers
PrivateComplex<private>(data:string)<decides><suspends>:string = ProcessData(data)


#! Valid expression
# 245. Function with visibility and override specifier
VirtualMethod<public>()<override>:void = BaseMethod()


#! Valid expression
# 246. Function with visibility and transacts specifier
UpdateState<public>(value:int)<transacts>:void = SetValue(value)


#! Valid expression
# 247. Function with behavior specifiers after parameters only
Calculate(a:int, b:int)<decides>:int = a + b


#! Valid expression
# 248. Function with override specifier after parameters
HandleEvent(event:Event)<override>:void = ProcessEvent(event)


#! Valid expression
# 249. Function with abstract specifier
DefineInterface()<abstract>:void = {}


#! Valid expression
# 250. Function with final specifier
SealedMethod()<final>:int = 100


#! Valid expression
# 251. Function with native specifier
SystemCall()<native>:void = {}


#! Valid expression
# 252. Function with inline specifier
FastOperation()<inline>:int = 1


#! Valid expression
# 253. Function with reads specifier
GetValue()<reads>:int = internalValue


#! Valid expression
# 254. Function with writes specifier
SetValue(x:int)<writes>:void = { internalValue = x }


#! Valid expression
# 255. Function with allocates specifier
CreateObject()<allocates>:Object = Object{}


#! Valid expression
# 256. Complex function with all specifier types
ComplexMethod<public>(input:string)<decides><suspends><override>:string = ProcessInput(input)


#! Valid expression
# 257. Function with scoped visibility and parameters
ScopedProcess<scoped>(x:int, y:string)<decides>:logic = ValidateParams(x, y)


#! Valid expression
# 258. Function with protected visibility and return type
ProtectedGetter<protected>():?int = GetInternalValue()


#! Valid expression
# 259. Function with public visibility in compound expression
{ PublicAPI<public>():void = {}; PrivateHelper<private>():void = {} }


#! Valid expression
# 260. Function with visibility and complex parameters
DataProcessor<public>(items:[]string, config:ProcessConfig)<decides><transacts>:[]Result = ProcessItems(items, config)


#! Valid expression
# 261. Function with visibility and callback parameter
EventHandler<public>(callback:EventCallback)<suspends>:void = RegisterCallback(callback)


#! Valid expression
# 262. Function with visibility and optional parameters
OptionalProcessor<public>(required:int, optional:?string)<decides>:string = FormatResult(required, optional)


#! Valid expression
# 263. Function with internal visibility and array return type
InternalData<internal>():[]byte = GetRawData()


