{x:= :int;
 x#1 => x#1 = 1 | 2;
 (x, operator'>'[x, 0])[0];
 let (x#2:= operator'+'(x, 1))
 in
   if (x#5:= (x#6 => {do 0 = x#6;
                      1})[x#2])
   then x#5
   else (if (x#4:= (x#7 => {do 1 = x#7;
                            1})[x#2])
         then x#4
         else (if (x#3:= (x#8 => {do n = x#8;
                                  operator'-'(n, 1)})[x#2])
               then x#3
               else :false));
 {x;
  y};
 if (x#9:= x) then x#9 else y;
 x:= int[e];
 f:= x#10 => {x:= int[x#10];
              e};
 operator'+'(x, y);
 operator'<'[x, y]}
